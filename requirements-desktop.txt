# Direct desktop packaging requirements. Release jobs install the generated,
# hash-checked platform locks instead; refresh both after changing either input:
#   scripts/lock_desktop_dependencies.sh
pyinstaller>=6.11,<7
pywebview>=5.3,<6
